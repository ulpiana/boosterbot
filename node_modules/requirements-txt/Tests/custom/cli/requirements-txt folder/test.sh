#!/usr/bin/env bash
{ set +x; } 2>/dev/null

path="${BASH_SOURCE[0]%/*}"
requirements="$(requirements-txt "$path")"
[[ $requirements == "pkgname1
pkgname2" ]] && exit 0

( set -x; requirements-txt "$path" ); exit 1
