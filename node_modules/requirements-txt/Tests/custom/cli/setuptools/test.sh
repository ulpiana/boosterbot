#!/usr/bin/env bash
{ set +x; } 2>/dev/null

path="${BASH_SOURCE[0]%/*}"/module.py
requirements="$(requirements-txt "$path")"
[[ $requirements == "setuptools" ]] && exit 0

( set -x; requirements-txt "$path" ); exit 1
